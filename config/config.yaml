python: "3.10"
random_seed: 42

confidence_min: 0.6
use_sample_data: true

paths:
  data: "data/sample_fb_ads.csv"
  reports: "reports"
  schema: "config/schema.yaml"

thresholds:
  ctr_drop_pct: 10.0
  roas_drop_pct: 10.0
